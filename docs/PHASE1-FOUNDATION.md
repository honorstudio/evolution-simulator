# Phase 1: 기초 시스템 (Foundation)

> MVP (Minimum Viable Product) - 핵심 시스템 구축

## 목표

빈 세계에서 단세포 생물이 탄생하고, 기본적인 생존과 번식을 하며, 간단한 진화가 일어나는 것을 관찰할 수 있는 최소 기능 버전을 구현합니다.

---

## 1. 프로젝트 설정

### 1.1 개발 환경 구성
```
evolution-simulator/
├── src/
│   ├── core/           # 핵심 엔진
│   ├── systems/        # 시스템 모듈
│   ├── entities/       # 생명체 클래스
│   ├── world/          # 세계/환경
│   ├── ui/             # 사용자 인터페이스
│   ├── utils/          # 유틸리티
│   └── workers/        # Web Worker
├── public/
├── docs/
└── tests/
```

### 1.2 기술 스택 초기 설정
- Vite + React + TypeScript
- Canvas 2D (Phase 1에서는 2D로 시작)
- 기본 상태 관리 (Zustand)

---

## 2. 환경 시스템 (Environment System)

### 2.1 세계 생성기 (World Generator)

무작위 지형 생성 시스템:

```typescript
interface WorldConfig {
  width: number;          // 세계 너비 (셀 단위)
  height: number;         // 세계 높이 (셀 단위)
  seed: number;           // 랜덤 시드 (재현 가능)
}

interface Cell {
  x: number;
  y: number;
  terrain: TerrainType;   // 지형 타입
  elevation: number;      // 고도 (0-1)
  water: number;          // 수분량 (0-1)
  temperature: number;    // 온도 (켈빈)
  nutrients: number;      // 영양분 (0-1)
}

enum TerrainType {
  DEEP_OCEAN,      // 심해
  SHALLOW_OCEAN,   // 얕은 바다
  BEACH,           // 해변
  PLAIN,           // 평원
  FOREST,          // 숲
  MOUNTAIN,        // 산
  DESERT,          // 사막
  TUNDRA           // 동토
}
```

### 2.2 지형 생성 알고리즘

**Perlin Noise / Simplex Noise 사용**:

```
1. 고도 맵 생성 (Elevation)
   - 다중 옥타브 노이즈로 자연스러운 지형
   - 고도에 따라 기본 지형 결정

2. 수분 맵 생성 (Moisture)
   - 바다와의 거리 기반
   - 추가 노이즈로 변화 추가

3. 온도 맵 생성 (Temperature)
   - 위도 기반 기본 온도
   - 고도에 따른 감소
   - 해류 영향 (선택적)

4. 생태계 결정
   - 고도 + 수분 + 온도 조합으로 지형 타입 결정
```

### 2.3 대기 시스템 (Atmosphere)

```typescript
interface Atmosphere {
  oxygen: number;         // O₂ 농도 (0-1, 기본 0.21)
  carbonDioxide: number;  // CO₂ 농도 (0-1)
  nitrogen: number;       // N₂ 농도 (0-1)
  humidity: number;       // 평균 습도 (0-1)
  pressure: number;       // 기압 (atm)
}
```

Phase 1에서는 전역 대기로 단순화, Phase 3에서 지역별 차이 추가.

### 2.4 시간 시스템 (Time)

```typescript
interface TimeSystem {
  tick: number;           // 현재 틱 (최소 시간 단위)
  speed: number;          // 시뮬레이션 속도 (1x ~ 10000x)
  dayLength: number;      // 하루 = n틱
  yearLength: number;     // 1년 = n일

  // 계산된 값
  currentDay: number;
  currentYear: number;
  dayPhase: 'dawn' | 'day' | 'dusk' | 'night';
}
```

---

## 3. 단세포 생물 시스템

### 3.1 최초 생명 탄생

**원시 수프 시뮬레이션**:

```typescript
interface PrimordialSoup {
  organicMolecules: number;  // 유기 분자 농도
  energy: number;            // 가용 에너지 (번개, 열수구 등)

  // 생명 탄생 확률 계산
  calculateSpawnChance(): number;
}
```

조건 충족 시 무작위로 최초 단세포 생물 탄생:
- 적절한 온도 (액체 상태의 물)
- 충분한 유기 분자
- 에너지 공급원 존재

### 3.2 단세포 생물 구조

```typescript
interface Organism {
  id: string;
  position: { x: number; y: number };

  // 물리적 속성
  size: number;           // 크기
  energy: number;         // 현재 에너지
  maxEnergy: number;      // 최대 에너지
  age: number;            // 나이 (틱)
  lifespan: number;       // 수명 (틱)

  // 유전자
  genome: Genome;

  // AI 두뇌
  brain: NeuralNetwork;

  // 상태
  isAlive: boolean;
}

interface Genome {
  // 신체 유전자
  metabolismRate: number;   // 대사율 (에너지 소비 속도)
  speed: number;            // 이동 속도
  senseRange: number;       // 감지 범위
  size: number;             // 기본 크기

  // 번식 유전자
  reproductionCost: number; // 번식에 필요한 에너지
  mutationRate: number;     // 돌연변이 확률

  // 외형 유전자 (Phase 1에서는 색상만)
  color: { r: number; g: number; b: number };
}
```

### 3.3 기본 AI (신경망)

Phase 1에서는 간단한 신경망 구조:

```
입력층 (5 뉴런)           은닉층 (4 뉴런)       출력층 (3 뉴런)
┌─────────────┐           ┌─────────┐           ┌──────────┐
│ 에너지 레벨 │           │         │           │ 이동 방향│
│ 가장 가까운 │           │   ○○    │           │ (각도)   │
│ 먹이 방향   │  ──────▶  │   ○○    │  ──────▶  │ 이동 속도│
│ 가장 가까운 │           │         │           │ 분열 여부│
│ 위험 방향   │           │         │           │          │
└─────────────┘           └─────────┘           └──────────┘
```

### 3.4 행동 시스템

```typescript
enum Action {
  MOVE,       // 이동
  EAT,        // 먹기 (영양분 흡수)
  REPRODUCE,  // 분열 (무성생식)
  IDLE        // 대기
}

// 매 틱마다 실행
function update(organism: Organism) {
  // 1. 감각 정보 수집
  const inputs = gatherSensoryInput(organism);

  // 2. 신경망으로 결정
  const outputs = organism.brain.forward(inputs);

  // 3. 행동 실행
  executeAction(organism, outputs);

  // 4. 에너지 소비 및 노화
  consumeEnergy(organism);
  age(organism);

  // 5. 죽음 체크
  checkDeath(organism);
}
```

---

## 4. 번식 및 진화 시스템

### 4.1 무성 번식 (분열)

```typescript
function reproduce(parent: Organism): Organism {
  // 에너지 비용 지불
  parent.energy -= parent.genome.reproductionCost;

  // 유전자 복제 + 돌연변이
  const childGenome = mutate(parent.genome);

  // 신경망 복제 + 돌연변이
  const childBrain = mutateBrain(parent.brain);

  // 자식 생성
  return createOrganism(childGenome, childBrain, parent.position);
}
```

### 4.2 돌연변이 시스템

```typescript
function mutate(genome: Genome): Genome {
  const mutated = { ...genome };

  for (const gene of Object.keys(mutated)) {
    if (Math.random() < genome.mutationRate) {
      // 유전자 값 랜덤 변이
      mutated[gene] *= (0.8 + Math.random() * 0.4); // ±20% 변이
    }
  }

  return mutated;
}

function mutateBrain(brain: NeuralNetwork): NeuralNetwork {
  const mutated = brain.clone();

  // 가중치 랜덤 변이
  for (const weight of mutated.weights) {
    if (Math.random() < 0.1) {
      weight += (Math.random() - 0.5) * 0.2;
    }
  }

  return mutated;
}
```

---

## 5. 기본 UI 시스템

### 5.1 메인 화면 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│  Evolution Simulator                    [⏸️][▶️][⏩][⏩⏩]  │
├─────────────────────────────────────────────────────────────┤
│                                              │ 📊 통계      │
│                                              │              │
│                                              │ 년도: 1,234  │
│                                              │ 생물: 5,678  │
│           [메인 시뮬레이션 뷰]               │ 종: 12       │
│                                              │              │
│                                              │ ──────────   │
│                                              │ 🌡️ 환경      │
│                                              │ 온도: 25°C   │
│                                              │ O₂: 21%     │
├──────────────────────────────────────────────┴──────────────┤
│  [줌: ━━━●━━━]  [좌표: 123, 456]  [선택: 없음]              │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 필수 UI 컴포넌트

- **시간 컨트롤**: 일시정지, 재생, 속도 조절
- **맵 뷰어**: 팬, 줌 기능
- **기본 통계 패널**: 생물 수, 년도, 환경 정보
- **개체 선택**: 클릭으로 개체 선택 및 기본 정보 표시

### 5.3 개체 정보 패널 (기본)

```
┌─────────────────────────────┐
│ 🔬 개체 #1234               │
├─────────────────────────────┤
│ 에너지: ████████░░ 78%     │
│ 나이: 234 틱                │
│ 세대: 15                    │
│                             │
│ [유전자]                    │
│ 속도: 1.2                   │
│ 크기: 0.8                   │
│ 대사율: 1.1                 │
│                             │
│ [🔍 상세보기]               │
└─────────────────────────────┘
```

---

## 6. 저장/불러오기 시스템 (기본)

### 6.1 저장 데이터 구조

```typescript
interface SaveData {
  version: string;
  timestamp: number;

  // 세계 상태
  world: {
    config: WorldConfig;
    cells: Cell[];
    atmosphere: Atmosphere;
  };

  // 시간 상태
  time: TimeSystem;

  // 생물 데이터
  organisms: Organism[];

  // 통계
  statistics: {
    totalBorn: number;
    totalDied: number;
    speciesCount: number;
  };
}
```

### 6.2 IndexedDB 저장

```typescript
// 자동 저장 (5분마다)
// 수동 저장 (사용자 요청)
// 새 게임 시작
// 저장 목록 관리
```

---

## 7. 성능 최적화 (기본)

### 7.1 공간 분할

```typescript
// 쿼드트리로 충돌/근접 검사 최적화
class QuadTree {
  bounds: Rectangle;
  objects: Organism[];
  children: QuadTree[];

  insert(organism: Organism): void;
  query(range: Rectangle): Organism[];
}
```

### 7.2 렌더링 최적화

- 화면 밖 개체는 렌더링 스킵
- 줌 아웃 시 개체를 점으로 단순화
- requestAnimationFrame 사용

---

## 8. Phase 1 완료 기준

### 필수 기능 체크리스트

- [x] 무작위 세계 생성 (지형, 물, 온도) ✅ 2024-11-28
- [x] 단세포 생물 탄생 및 생존 ✅ 2024-11-28
- [x] 기본 AI (먹이 찾기, 이동) ✅ 2024-11-28
- [x] 무성 번식 및 돌연변이 ✅ 2024-11-28
- [x] 자연선택 (적응 실패 시 사망) ✅ 2024-11-28
- [x] 시간 조절 (일시정지, 속도 조절) ✅ 2024-11-28
- [x] 기본 관찰 도구 (개체 선택, 정보 표시) ✅ 2024-11-28
- [x] 저장/불러오기 ✅ 2024-11-28
- [x] 새 게임 시작 ✅ 2024-11-28

### 성공 지표

1. 최소 1000개의 단세포 생물 동시 시뮬레이션
2. 60fps 유지 (1000개체 기준)
3. 세대를 거치며 눈에 띄는 진화 발생
4. 환경에 따른 생존율 차이 관찰 가능

---

## 9. 다음 단계 (Phase 2 예고)

Phase 1 완료 후:
- 다세포 생물로의 진화
- 프로시저럴 외형 생성
- 성선택 시스템
- 식물/동물 분화

---

## 참고 자료

- [Perlin Noise 알고리즘](https://en.wikipedia.org/wiki/Perlin_noise)
- [NEAT 알고리즘](http://nn.cs.utexas.edu/downloads/papers/stanley.ec02.pdf)
- [진화 시뮬레이션 사례](https://www.youtube.com/watch?v=r_It_X7v-1E)

---

## 📋 개발 진행 로그

### 2024-11-28 (Session 3 - Phase 1 완료) 🎉

#### ✅ 완료 작업

**1. 저장/불러오기 시스템 (`src/storage/`)**
- `types.ts` - 저장 데이터 타입 정의
- `StorageManager.ts` - IndexedDB 기반 저장소 관리
- `serialization.ts` - 게임 상태 직렬화/역직렬화
- `ManagerExtensions.ts` - OrganismManager 확장 메서드
- `GameSerialization.ts` - Game 클래스 확장 메서드
- `index.ts` - 모듈 익스포트

**2. UI 컴포넌트 추가**
- `panels/SaveLoadPanel.tsx` - 저장/불러오기 모달 UI
- `panels/SaveLoadPanel.css` - 스타일링
- 새 게임 버튼 추가 (App.tsx)

**3. 개체 클릭 선택 기능**
- `InputHandler.ts` - 클릭 콜백 지원
- `Renderer.ts` - handleClick(), findOrganismAtPosition()
- `GameCanvas.tsx` - onOrganismSelect prop 추가
- `Game.ts` - getRenderer() 메서드 추가
- `OrganismInfoPanel.tsx` - 실시간 개체 정보 표시

**4. 버그 수정 및 개선**
- `Brain.ts` - getWeights/getBiases/setWeights/setBiases 메서드 추가
- `Game.ts` - pause()/resume() 메서드 추가
- `OrganismInfoPanel.tsx` - 나이 소수점 정리 (Math.round)
- 클릭 반경 조정 (최소 100, genome.size * 50)

#### ✅ Phase 1 완료 기능
- 시뮬레이션 기본 동작 ✅
- 시간 조절 (일시정지, 0.5x~10x 속도) ✅
- 통계 패널 (실시간 업데이트) ✅
- 개체 클릭 선택 및 정보 표시 ✅
- 저장/불러오기 (IndexedDB) ✅
- 새 게임 시작 ✅

#### 📁 생성된 파일
```
src/storage/
├── types.ts
├── StorageManager.ts
├── serialization.ts
├── ManagerExtensions.ts
├── GameSerialization.ts
└── index.ts

src/ui/panels/
├── SaveLoadPanel.tsx
└── SaveLoadPanel.css
```

---

### 2024-11-28 (Session 2 - 추가 작업)

#### ✅ 추가 완료 작업

**1. React UI 시스템 (`src/ui/`)**
- `GameContext.tsx` - Game 인스턴스 Context
- `hooks/useGame.ts` - Game 훅
- `hooks/useStats.ts` - 실시간 통계 훅
- `hooks/useTimeControl.ts` - 시간 컨트롤 훅
- `components/GameCanvas.tsx` - PixiJS 캔버스
- `panels/TimeControlPanel.tsx` - 시간 컨트롤 UI
- `panels/StatsPanel.tsx` - 통계 패널
- `panels/OrganismInfoPanel.tsx` - 개체 정보 패널

**2. 버그 수정**
- `world/index.ts` - interface export 수정 (`export type`)
- `organism/Organism.ts` - require() → ES6 import 변경
- `renderer/colors.ts` - getElevationTint 색상 범위 수정
- `simulation/SimulationEngine.ts` - OrganismManager API 호환
- `Game.ts` - Renderer async 초기화 적용

#### ✅ 동작 확인
- 시작 화면 표시 ✅
- 시작하기 버튼 동작 ✅
- 시간 컨트롤 (일시정지, 속도) ✅
- 통계 실시간 업데이트 ✅ (틱, FPS, 생명체 수 등)
- 시뮬레이션 엔진 작동 ✅ (생명체 사망, 번식)
- 60fps 유지 ✅

#### ⚠️ 남은 작업
- 생명체/음식 렌더링 (캔버스에 표시)
- 저장/불러오기 (IndexedDB)
- 새 게임 시작 버튼

---

### 2024-11-28 (Session 1)

#### ✅ 완료된 작업

**1. 프로젝트 설정**
- npm 패키지 설치 완료
- Vite + React + TypeScript 환경 구성

**2. 세계 생성 시스템 (`src/world/`)**
- `WorldConfig.ts` - 월드 설정 상수
- `Tile.ts` - 타일/셀 타입 정의, 11가지 바이옴
- `TerrainGenerator.ts` - Simplex Noise 기반 지형 생성 (FBM 알고리즘)
- `Atmosphere.ts` - 대기 시스템 (O2, CO2, 온도)
- `World.ts` - 청크 기반 월드 관리

**3. 물리 엔진 (`src/physics/`)**
- `Vector2D.ts` - 2D 벡터 수학
- `constants.ts` - 물리 상수
- `Body.ts` - 물리 바디 클래스
- `Collision.ts` - 충돌 검사/해결
- `SpatialHash.ts` - 공간 해시 그리드 (성능 최적화)

**4. 렌더링 시스템 (`src/renderer/`)**
- `Renderer.ts` - PixiJS v8 기반 렌더러
- `Camera.ts` - 카메라 시스템 (줌, 드래그)
- `TerrainRenderer.ts` - 지형 렌더링, LOD
- `InputHandler.ts` - 마우스/키보드 입력
- `colors.ts` - 바이옴별 색상 팔레트

**5. 생명체 시스템 (`src/organism/`)**
- `Genome.ts` - 유전자 시스템 (돌연변이, 교배)
- `Brain.ts` - 간단한 신경망 (순전파)
- `Food.ts` - 음식 클래스
- `Organism.ts` - 생명체 클래스 (센서, AI, 번식)
- `OrganismManager.ts` - 생명체 관리자

**6. 시뮬레이션 엔진 (`src/simulation/`)**
- `config.ts` - 시뮬레이션 설정
- `Time.ts` - 시간 관리 (속도 조절, 일시정지)
- `Statistics.ts` - 통계 추적
- `SimulationEngine.ts` - 메인 엔진

**7. 게임 루프**
- `Game.ts` - 메인 게임 클래스
- `main-game.ts` - 독립 실행 진입점

#### 🔄 진행중인 작업
- 기본 UI (React 컴포넌트)
- 개체 선택 및 정보 표시

#### ❌ 남은 작업
- 저장/불러오기 (IndexedDB)
- 새 게임 시작 기능
- 전체 통합 테스트

#### ⚠️ 특이사항
- PixiJS v8 사용 (async 초기화 필요)
- 신경망은 커스텀 구현 (TensorFlow.js 미사용)
- 공간 해시 그리드로 충돌 검사 최적화 (O(n) 복잡도)

#### 📁 생성된 주요 파일
```
src/
├── world/          # 세계 시스템 (5개 파일)
├── physics/        # 물리 엔진 (5개 파일)
├── renderer/       # 렌더링 (6개 파일)
├── organism/       # 생명체 (6개 파일)
├── simulation/     # 시뮬레이션 (5개 파일)
├── Game.ts         # 메인 게임
└── main-game.ts    # 진입점
```

---

### 다음 세션에서 할 일

1. **기본 UI 구현**
   - React 컴포넌트 생성
   - 시간 컨트롤 패널
   - 통계 패널
   - 개체 선택 및 정보 표시

2. **저장/불러오기**
   - Dexie.js로 IndexedDB 연동
   - 세계 상태 직렬화

3. **통합 테스트**
   - 개발 서버 실행 확인
   - 기본 시뮬레이션 동작 확인

4. **버그 수정**
   - 타입 에러 해결
   - 모듈 import 경로 확인
