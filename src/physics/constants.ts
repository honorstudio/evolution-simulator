/**
 * 물리 엔진 상수
 *
 * 게임 세계의 물리 법칙을 정의합니다.
 * 이 값들을 조정하면 게임의 느낌이 완전히 바뀝니다.
 */
export const PHYSICS = {
  /**
   * 중력 가속도
   * 수중 환경이라 현실보다 약하게 설정
   * 값이 클수록 물체가 빨리 아래로 떨어집니다
   */
  GRAVITY: 0.1,

  /**
   * 물 저항 계수
   * 0.98 = 매 프레임마다 속도의 2%가 감소
   * 1에 가까울수록 저항이 적고, 0에 가까울수록 저항이 큽니다
   */
  WATER_DRAG: 0.98,

  /**
   * 공기 저항 계수
   * 물보다 저항이 적어서 값이 더 큽니다
   */
  AIR_DRAG: 0.99,

  /**
   * 지면 마찰 계수
   * 바닥에 닿았을 때 미끄러지는 정도
   */
  FRICTION: 0.95,

  /**
   * 최대 속도 제한
   * 생물체가 너무 빨리 움직이지 않도록 제한
   * 단위: 픽셀/프레임
   */
  MAX_VELOCITY: 10,

  /**
   * 반발 계수 (Coefficient of Restitution)
   * 0 = 완전 비탄성 충돌 (튕기지 않음)
   * 1 = 완전 탄성 충돌 (에너지 손실 없이 튕김)
   * 0.5 = 충돌 시 속도의 50%로 튕겨나감
   */
  BOUNCE: 0.5,
} as const;

/**
 * 충돌 검사 최적화를 위한 공간 분할 상수
 */
export const SPATIAL_HASH = {
  /**
   * 그리드 셀 크기
   * 이 값이 작을수록 정밀하지만 메모리를 더 사용합니다
   * 일반적으로 게임 객체의 평균 크기의 2-3배가 적당합니다
   */
  CELL_SIZE: 100,
} as const;

/**
 * 시뮬레이션 성능 설정
 */
export const SIMULATION = {
  /**
   * 고정 시간 간격 (밀리초)
   * 60fps = 1000ms / 60 ≈ 16.67ms
   * 물리 계산의 일관성을 위해 고정된 시간 간격 사용
   */
  FIXED_TIMESTEP: 1000 / 60,

  /**
   * 최대 프레임 스킵
   * 게임이 느려져도 물리 계산이 너무 많이 누적되지 않도록 제한
   */
  MAX_FRAME_SKIP: 5,
} as const;
